// アフィリエイト URL ユーティリティ
// V1.5 でアフィリエイトパラメータを各サービスに申請後、ここに追加予定

export function buildTabelogUrl(baseUrl: string | null): string | null {
	if (!baseUrl) return null;
	// V1.5 でアフィリエイトパラメータを追加予定
	// 現時点ではそのまま返す
	return baseUrl;
}

export function buildGurunaviUrl(baseUrl: string | null): string | null {
	if (!baseUrl) return null;
	// V1.5 でアフィリエイトパラメータを追加予定
	// 現時点ではそのまま返す
	return baseUrl;
}
